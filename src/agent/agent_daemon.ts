// Deprecated: legacy prototype daemon moved to ops/legacy/agent_daemon.ts
//
// The current server uses queue_* + task_graph + orchestrator_tick (+ execution_bind_tick/agent_dispatch_tick)
// for autonomous operation.
//
// Keeping a stub here prevents accidental usage and keeps imports (if any) from breaking.
export {};
